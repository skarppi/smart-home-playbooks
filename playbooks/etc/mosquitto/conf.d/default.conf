connection bridge
address {{mqtt_remote_host}}:{{mqtt_remote_port}}
remote_username {{mqtt_remote_username}}
remote_password {{mqtt_remote_password}}
topic # both 1 "" {{mqtt_remote_prefix}}
bridge_capath /etc/ssl/certs
